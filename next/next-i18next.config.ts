const i18n = {
    i18n: {
        locales: ['fa', 'en'],
        defaultLocale: 'fa',
        localeDetection: false as false, // ✅ Fix type error
    },
    trailingSlash: true,
};

export default i18n;
